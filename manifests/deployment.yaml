apiVersion: apps/v1
kind: Deployment
metadata:
  name: addtask-api
spec:
  selector:
    matchLabels:
      app: addtask-api
  replicas: 2
  template:
    metadata:
      labels:
        app: addtask-api
    spec:
      containers:
      - name: addtask-api
        image: mdcontainerregistry.azurecr.io/addtask-api:v3
        ports:
        - containerPort: 8000
        env:
        - name: CONNECTION_STRING
          value: "{ODBC Driver 18 for SQL Server};Server=tcp:md-mssql-01.database.windows.net,1433;Database=md-sql-db-01;Uid=sqladmin;Pwd=Sql@123456;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;"



